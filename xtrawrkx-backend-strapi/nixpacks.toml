[phases.setup]
nixPkgs = ["nodejs-20_x", "python3", "make", "gcc"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node scripts/debug-env.js && npm start"

