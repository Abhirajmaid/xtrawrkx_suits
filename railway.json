{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "cd xtrawrkx-backend && npm install && npm run db:generate && npm run build"
    },
    "deploy": {
        "startCommand": "cd xtrawrkx-backend && npm start",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}